/**
 * Constants for workspace tabs behavior
 */

/** Tooltip delay for delete button (ms) */
export const DELETE_TOOLTIP_DELAY = 500;

/** Tooltip delay for workspace name (ms) */
export const WORKSPACE_TOOLTIP_DELAY = 600;
